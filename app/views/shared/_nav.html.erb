<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-exa
mple-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>"><strong class="school-logo"><%= Option.last.logo %></strong></a>
      <div class="navbar-text"><strong class=""><%= app_name %></strong></div>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if not @current_user.guest? %>
            <li><%= link_to 'Dashboard', show_user_path(@current_user) %></li>
        <% end %>
        <li class="dropdown"> <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Help <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'About', about_path %></li>
            <li><%= link_to 'User Docs', user_doc_path %></li>
            <li><%= link_to 'Developer Docs', developer_doc_path %></li>
          </ul>
        </li>
        <% if @current_user.guest? %>
            <li><%= link_to 'Signup', signup_form_path %></li>
            <li><%= link_to 'Login', login_form_path %></li>
        <% elsif @current_user.participant? or @current_user.owner? %>
            <% if @current_user.admin? %>
                <li class="dropdown"> <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Admin <span class="caret"></span></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to 'Events', show_all_events_path %></li>
                    <li><%= link_to 'Locations', show_all_locations_path %></li>
                    <li><%= link_to 'Users', show_all_users_path %></li>
                  </ul>
                </li>
            <% end %>
            <li class="dropdown"> <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><i class="glyphicon glyphicon-user" aria-hidden="true"></i> <%= @current_user.email %> <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Account', edit_user_form_path(@current_user) %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Logout', logout_path %></li>
              </ul>
            </li>
        <% end %>
      </ul>
    </div> <!-- /.navbar-collapse -->
  </div> <!-- /.container-fluid -->
</nav>
