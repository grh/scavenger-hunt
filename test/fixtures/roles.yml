guest:
  name: guest
  permissions: view_public_pages, create_account, login

participant:
  name: participant
  permissions: view_public_pages, manage_own_account, view_event, join_event, visit_location, leave_event, logout

owner:
  name: owner
  permissions: manage_own_events, manage_own_locations

admin:
  name: admin
  permissions: manage_all_events, manage_all_locations, manage_all_users, manage_all_options